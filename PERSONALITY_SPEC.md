# 性格推定機能 リニューアル仕様（科学版）

最終更新: 2026-06-08

## 0. 方針サマリ

現行の「LLMに性格を自由記述させる」方式を、**検証済みBig Five尺度（TIPI-J）を骨格にした
証拠ベースの推定**へ置き換える。アンケート回答は集まらない前提なので、**主力はチャットログ
からの自動推論**とし、自己申告（TIPI-J）は精度のアンカー（較正用）として併置する。

| レイヤー | 役割 | 実装 |
|---|---|---|
| A. 自己申告（TIPI-J 10問） | 科学的アンカー。本人が答えたときだけ「妥当」と言える唯一の出力 | `/personality` コマンド（main.py） |
| B. ログ推論（主力） | TIPI-Jの各項目をLLM＋言語特徴量で本人に代わって採点 | `batch/analyze_personality.py` / `analyze_nonbooster.py` |
| C. 提示・倫理 | 「推定」と明示・確信度・オプトアウト | `/myprofile`・`format_profile`・`/privacy` |

トーン方針 = **ハイブリッド**：科学的バックボーン（Big Five）は保持し、見せ方は親しみやすく。
MBTI型は「お遊び」併記としてのみ可（科学的根拠としては使わない）。

## 1. なぜこの設計か（科学的根拠）

- **検証済み尺度を使う**のが心理測定で言う「科学的に正確」。日本語では **TIPI-J**
  （小塩・阿部・Cutrone, 2012, パーソナリティ研究）が10項目・約1分で再検査信頼性/妥当性検証済み。
- **テキストからのLLM性格推定の実精度は低い**：自己申告との相関 r≈0.29（範囲0.22–0.33）、
  近年研究では r≤0.27。中〜高に系統的に過大評価。外向性・神経症傾向は特に当てにくい。
  → だから**数値スコア（73/100等）は出さない**。3段階バンド＋確信度＋根拠引用に留める。
- **MBTI（16型）は信頼性・妥当性が低い**ため科学的基盤としては不採用。娯楽用途の併記のみ。
- **バーナム効果回避**：誰にでも当たる曖昧表現を禁止。各因子に必ず**実発言の根拠引用**を要求し、
  出せない因子は null。

## 2. データスキーマ（users コレクション）

```jsonc
profile.bigfive = {
  "openness":          { "band": "高|中|低", "confidence": "high|mid|low", "evidence": "根拠の実発言", "score": 0-100 },
  "conscientiousness": { ... },
  "extraversion":      { ... },
  "agreeableness":     { ... },
  "neuroticism":       { ... },
  "method": "inferred",
  "data_points": 42,                  // 推論に使った発言数
  "analyzed_at": "2026-06-08T..."
}
profile.bigfive_self = {              // /personality 回答時のみ
  "openness": { "band": "...", "raw": 1-7 }, ...,
  "method": "self_report",
  "answered_at": "..."
}
```
`score`(0-100) は内部・較正用に保持するが **UIには出さない**（バンドのみ表示）。
口調系（`tone`/`vocabulary`/`communication_style`）は**今回の対象外**（文体模倣=stylometryで別物・現状維持）。

## 3. レイヤーB：ログ推論ロジック（主力・本仕様の中核）

3段構成。**Python製の決定論的特徴量**と**LLM項目採点**を組み合わせる（personality computing方式）。

### データ源 — 全メンバー対象（2026-06-08 拡張）
ブースター概念は廃し**全メンバーを対象**にする。発言は `messages` コレクション（全チャット発言）
を主データ源、`butler_history`（メイド会話）を補助/フォールバックにする。これにより
**メイドと話さない人も普段のチャットからBig Five推定できる**。
- 対象選定: `xp>0`・`personality_optout != True` の全員。メイドと活発な人(`conv_count>=30`)を最優先し、
  残り枠を「未/久しく分析していない人」でローテーション補充（`needs_rotation` = 直近 `ROTATION_DAYS=5` 日内は除外）。
- タイムアウト対策: 1バッチ最大 `MAX_USERS_PER_RUN=25` 人。発言 `MIN_UTTERANCES=15` 未満は枠を消費せずスキップ。
- 全員が数日かけて巡回分析される（毎回全員叩かない）。

### Stage 1 — 言語特徴量（Python・再現性あり・LLM不要）
収集した発言から日本語対応の指標を算出し、因子の事前シグナルにする。

| 因子 | シグナル（例） | 信頼度 |
|---|---|---|
| 外向性 (E) | 発言数・平均文長・感嘆/絵文字率・メンション率・会話を振る頻度 | 高 |
| 協調性 (A) | 感謝/称賛語（ありがとう/助かる/いいね/すごい）・攻撃語の少なさ | 中 |
| 開放性 (O) | 語彙多様性(異なり語/延べ語)・好奇心語（なんで/気になる/知りたい）・話題の幅 | 中 |
| 神経症傾向 (N) | ネガ感情語（不安/しんどい/つらい/無理）・過剰な謝罪 | 低（注意） |
| 誠実性 (C) | 文の完結度・計画語（予定/明日/ちゃんと） | 低（注意） |

E は言語シグナルが最も効く。N/C はチャットからは弱いので**確信度を上げすぎない**。

### Stage 2 — TIPI-J項目のLLM採点（自由記述を廃止）
TIPI-Jの10項目（5因子×2項目、うち各因子1項目は逆転）を、LLMが**本人に代わって**
ログから 1–7 で採点。各採点に**根拠の実発言引用**を必須化。証拠が無い項目は null。
Stage1の言語シグナルをヒントとして渡す。

TIPI-J項目（採点対象。「私は自分自身を、○○ と思う」形式）:
1. 活発で、外向的だと思う（E）
2. 他人に不満をもち、もめごとを起こしやすいと思う（A逆転）
3. しっかりしていて、自分に厳しいと思う（C）
4. 心配性で、うろたえやすいと思う（N）
5. 新しいことが好きで、変わった考えをもつと思う（O）
6. ひかえめで、おとなしいと思う（E逆転）
7. 人に気をつかう、やさしい人間だと思う（A）
8. だらしなく、うっかりしていると思う（C逆転）
9. 冷静で、気分が安定していると思う（N逆転）
10. 発想力に欠けた、平凡な人間だと思う（O逆転）

因子スコア = 各因子2項目（逆転は 8−x で反転）の平均。

### Stage 3 — バンド化＋確信度
- 因子スコア(1–7)→ 内部 score(0–100)→ バンド（低 ≤3.5 / 中 / 高 ≥5.0、暫定閾値）
- **確信度** = f(data_points, 当該因子の根拠数, Stage1とStage2の整合):
  - high: data_points≥80 かつ 根拠あり かつ N/C以外
  - mid : data_points≥30 かつ 根拠あり
  - low : それ未満、または N/C、またはStage1/2が不一致
- `/personality` の自己申告があれば**それを正**とし、推論は補足として併置（食い違いは表示）。

### Stage 4 — 安全マージ（既知バグの恒久修正）
現行 `merge_profiles`(L234) は空/薄レスポンスで蓄積を上書き破壊し conv_count もリセットする。
新ルール:
- 因子ごとに「**新しい確信度 ≥ 既存の確信度**」のときのみ更新。null や low で既存の非null/highを潰さない。
- `conv_count` は **confident な因子が1つ以上保存できたときだけ** unset（失敗時はトリガーを残す）。
- 全因子 null（データ不足）なら**何も書かない**（skip）。

## 4. レイヤーA：/personality（TIPI-J 自己申告）

- スラッシュコマンド `/personality`：Discord UI（Select×10 もしくは Modal）でTIPI-J 10問に回答（1–7）。
- 採点して `profile.bigfive_self` に保存。`/myprofile` で推論結果と並べて表示。
- 任意・再受験自由（性格は変動するため再検査前提を明記）。
- 回答が集まらない前提なので必須にはしない。集まった分は推論の較正・検証に使える。

## 5. レイヤーC：提示・倫理

- 名称：**ログ推論(レイヤーB)** は「性格**診断**」→「性格**推定（参考）**」に変更（臨床的断定を避ける）。
  **自己申告(レイヤーA `/personality`)** は検証済み尺度なので、ハイブリッド方針のもと娯楽寄りの
  「性格**診断**」表記を許容（コマンド説明・View見出し・フッターで使用）。
- **バンド閾値（科学的refinement・未対応/follow-up）**: 現在は固定閾値（≤3.5 低 / ≥5.0 高）。
  本来は TIPI-J の出版済み母集団規範（Oshio et al. 2012 の因子別 平均±SD）で規範参照すべき
  （日本サンプルは中点4.0からズレる因子がある／LLM推定は高めに偏るので固定5.0は高バンドを過大化）。
  採用時は**出典の規範表を必ず取得してから**ハードコードすること（記憶からの再構成は禁止）。
- `/myprofile` 注記：「会話ログからのAI推定です。正確な診断ではありません。`/personality` で
  本人回答に基づく結果も見られます」。
- バンド表示（数値は出さない）＋因子ごとの確信度バッジ（◎/○/△）＋根拠引用。
- ハイブリッド見せ方：各因子に親しみやすい一言ラベル（例 外向性=高→「社交的・話題を振る」）。
  任意で「タイプ風ニックネーム」をお遊び併記可（科学主張はしない）。
- **同意/オプトアウト**：非ブースターは現状無断で受動分析。第三者の性格も群チャットから推定している。
  `/privacy`（または `/personality` 内トグル）で推定停止・非表示を選べるようにする。停止者は
  バッチ対象から除外（`users.personality_optout: true`）。

## 6. 影響ファイル

- `batch/analyze_personality.py` … Stage1–4 実装（ブースター・主力）
- `batch/analyze_nonbooster.py` … 同方式に揃える（要約ベースなのでStage1は限定的）
- `main.py` … `/personality`・`/privacy`・`format_profile`・`_build_mimic_profile`・`/myprofile` 更新
- `_build_mimic_profile` は口調模倣が主目的なので bigfive は軽く反映する程度（喋り方は変えない）

## 7. 出典

- 小塩真司・阿部晋吾・Cutrone, P.「日本語版Ten Item Personality Inventory（TIPI-J）作成の試み」
  パーソナリティ研究 21(1), 2012. https://www.jstage.jst.go.jp/article/personality/21/1/21_40/_article/-char/ja/
- 項目本文: https://oshio.w.waseda.jp/archives/617
- Peters & Matz (2024) LLMはSNS利用者の心理特性を推定可能（r≈0.29）: https://pmc.ncbi.nlm.nih.gov/articles/PMC11211928/
- "Mind Reading or Misreading? LLMs on the Big Five" (2025, r≤0.27・過大評価傾向): https://arxiv.org/html/2511.23101
